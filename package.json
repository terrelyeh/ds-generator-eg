{
  "name": "ds-generator-eg",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w spechub",
    "build": "npm run build -w spechub",
    "lint": "npm run lint -w spechub"
  }
}
